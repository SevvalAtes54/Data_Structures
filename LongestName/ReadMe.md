
# LongestName

LongestName is a C program that allows the user to enter a list of student names and then find the longest name in the list. The program uses a linked list data structure to store the student names.

## Features

- Allows the user to enter multiple student names.
- Finds and displays the longest name from the entered names.
- Handles an empty list scenario gracefully.

## How to Use
